
const { readFile } = require('./File');

module.exports = () => {
    readFile('../data/help.txt').then((res) => {
        const helpTxt = res.replaceAll('\\u001b', '\u001b');
        console.log(helpTxt);
    }).catch((err) => {
        console.log(err);
    })
}
